---
description: Show the read-only issue shortlist and optionally hand it to issue-loop. Explicit invocation only.
argument-hint: optional — "batch=N" to change the 10, "mine" to restrict to issues assigned to me
allowed-tools: Bash, Read, Grep, Glob, AskUserQuestion, ToolSearch, mcp__ccd_session_mgmt__set_session_title
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/issue-triage/SKILL.md` completely and follow
it with `$ARGUMENTS` as the invocation text.
